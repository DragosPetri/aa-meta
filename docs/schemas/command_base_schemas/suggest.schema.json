{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "attach-meta/base/suggest",
  "description": "Base schema for the suggest command. Kind is the first positional (required); remaining positionals are kind-specific args.",
  "type": "object",
  "properties": {
    "kind": { "type": "string", "x-positional": true }
  },
  "required": ["kind"]
}
