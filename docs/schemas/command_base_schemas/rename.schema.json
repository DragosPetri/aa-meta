{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "attach-meta/base/rename",
  "description": "Base schema for the rename command. --to is required.",
  "type": "object",
  "properties": {
    "to": { "type": "string" }
  },
  "required": ["to"]
}
